
[КОД ДЛЯ АНАЛІЗУ]
function process(d) {
var r = [];
for (var i = 0; i < d.length; i++) {
if (d[i].active == true) {
if (d[i].age > 18) {
r.push(d[i].name + " - " + d[i].age);
}
}
}
return r;
}

[ЗАВДАННЯ]
Проведи ретельний Code Review цього фрагменту коду. Тобі потрібно:
1. Виявити та чітко класифікувати щонайменше 3 проблеми/code smells (незрозумілі назви, застарілий синтаксис, надлишкова вкладеність тощо).
2. Запропонувати сучасний, чистий та декларативний варіант цього коду, написаний за стандартами ES6+ (використовуючи декларативні методи масивів). Рефакторинг НЕ повинен змінювати початкову логіку роботи функції (поведінка має залишитися ідентичною).

[ФОРМАТ ВІДПОВІДІ]
Відповідь має бути чітко структурованою і складатися з трьох блоків:
- Блок 1: Список знайдених проблем (з коротким обґрунтуванням, чому це погано).
- Блок 2: Відрефакторений код (Clean Code ES6+).
- Блок 3: Пояснення внесених змін та чому новий варіант кращий за старий.
